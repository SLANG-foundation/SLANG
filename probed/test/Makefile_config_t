C = cc
F = -O0 -c -Wall -I .. -I /usr/include/libxml2 -g
#F = -pg -fprofile-arcs -ftest-coverage -c -Wall -I .. -I /usr/include/libxml2 -g
S = config_t.c ../util.c ../msess.c ../config.c
I = ../msess.h
L = -lrt -lxml2
#L = -pg -fprofile-arcs -ftest-coverage
O = $(S:.c=.o)
T = config_t

all: $T 

$T: $O
	$C $L -o $@ $^

.c.o:
	$C $F -o $@ $<

$O: $I

clean:
	rm $T $O 
